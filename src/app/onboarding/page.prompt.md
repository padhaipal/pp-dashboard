1.) Async server component (no "use client"). Gate: `const session = await auth(); if (!session) redirect("/login");` then `if (session.user.role !== "admin" && session.user.role !== "dev") redirect("/dashboard");`.
2.) Render the same header chrome as src/app/dashboard/page.tsx: page wrapper `min-h-screen bg-zinc-50 p-6` > `max-w-5xl mx-auto`; header row with h1 "Staff onboarding", a span `{external_id} ({role})`, a next/link Link to "/dashboard" labelled "Dashboard", and SignOutButton from ../dashboard/sign-out-button.
3.) Below the header mount the client component `<OnboardingConsole />` from ./onboarding-console. No props — everything it needs comes from the proxy at runtime.
4.) The backend paths this page depends on (geo-entities/search, geo-entities/:id, geo-entities/:id/descendants, users/staff-create, users/lookup, users/:id GET) are allowlisted for the admin role in src/app/api/proxy/[...path]/route.ts under the "Staff onboarding" comment; dev bypasses the allowlist.
