1.) Pure module (no React/DOM): TypeScript types mirroring the pp-sketch contract — `Metric = 'nipun_g2'|'nipun_g3'|'mpl_b'`, `Range = 30|90`, `GeoRef`, `Official`, `PublicProfile` (`/users/:id/public`), `Child`, `StudentChild`, `ScoresResponse` (`/geo-entities/:id/scores`), `SpotlightResponse` (`/geo-entities/:id/spotlight`).
2.) Constants: `METRICS` (labels "NIPUN grade 2 proxy" / "NIPUN grade 3 proxy" / "MPL-B proxy"), `RANGES = [30, 90]`, `DEFAULT_METRIC = 'nipun_g3'`, `DEFAULT_RANGE = 30`, `CHILD_NOUN` / `CHILD_OFFICER` (state→DGSE, district→BSA, block→BEO, school→Principal, teacher→Teacher, student→Teacher), `ACCENT = '#1d9edf'`. `ChildType` includes `"teacher"` (a school's children are its teachers = the referrers of its students; a teacher's children are students) and `GeoRef.type` is `GeoType | "teacher"` (teacher refs: id = the teacher's user id, code ""). `Child.students?` (teacher rows), `StudentChild.delta`. Student-modal shapes: `LiteracyTestScores` / `SnapshotTestScore` / `TestSnapshotPoint` (`TEST_KEY_OF` maps a metric to its test key), `UserMedia` / `MediaRow`; URLs `testScoresUrl`, `mediaUrl`, `audioUrl`.
3.) Colours: `UNCOVERED = '#a1a1aa'` (child with `using_lifteracy` false / bin none), `INCOMPLETE_FILL = '#000000'` + `INCOMPLETE_TOOLTIP = "District boundaries not yet available for this state"`, mvp2's `scoreRGB`/`scoreColor` (const arrow; red `239,68,68` → yellow `234,179,8` → green `34,197,94`, p in 0..1), `textOn`.
4.) `binColor(bin)`: high → `scoreColor(1)` = `rgb(34,197,94)`, mid → `scoreColor(0.5)` = `rgb(234,179,8)`, low → `scoreColor(0)` = `rgb(239,68,68)`, none → UNCOVERED. `binOf(passRate)` bins at ≥80 / ≥50 / <50 / null→none. `childFill(child)` = UNCOVERED unless using_lifteracy with a pass_rate, else `scoreColor(pass_rate/100)`. `nipColor(pct)` = mvp2's three-step palette (`#16a34a` ≥80, `#f59e0b` ≥50, `#dc2626`, UNCOVERED for null) used for every card, tint, headline figure and legend swatch — the continuous scale is for the map only.
4b.) `fmtPctInt` ("69%", mvp2's whole-number headline figures); `displayName(name, type)` title-cases UDISE UPPERCASE geo names for the location title, leaving school (and student) names untouched.
5.) URL builders (all through `/api/proxy`): `scoresUrl`, `csvUrl` (`…/scores.csv?metric=&range=`), `spotlightUrl`, `profileUrl`. `geoChildrenOf` / `studentChildrenOf` narrow `children` by `child_type`.
6.) Exact strings: `EMPTY_ROOT_TEXT = "No results yet — share your link to get started."`, `INACTIVE_LINK_TEXT = "This link is not active"`.
