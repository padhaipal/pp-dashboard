1.) "use client" `TeacherDashboard({ profile, incompleteStates })` — section-for-section port of the mvp2.html "report" layout (sandbox-global-map/mvp2.html, school level included). mvp2 is the source of truth for markup, Tailwind classes, spacing and wording; the ONE deliberate departure is the plant avatar (`/api/avatar/...`) in place of uploaded photos. Every number comes from pp-sketch through `/api/proxy/...` (public allowlist in src/app/api/proxy/[...path]/public-allowlist.ts). No dummy/hash-seeded data anywhere.
2.) Layout, top to bottom (mvp2 order): sticky bar (`max-w-6xl px-6 py-2.5`: Lifteracy logo `h-14 sm:h-16`, nav absolutely centred — Top / {Noun} Performance / {Officer} Spotlight / Your Profile —, `MvpLangToggle` EN/हिं, "⤓ Generate report" `px-5 py-2` accent `#1d9edf`); `#rep-top`; location title as the `<h1>` (`text-3xl sm:text-4xl`; ancestors + drill stack, `displayName` title-cases UDISE UPPERCASE names except schools, each passed through `t`, joined "  -  "); `MvpMetricToggle`; `RepKpis`; the map card (`h-[460px] rounded-2xl bg-[#eaf0f6]`, width `min(calc(100% - 3rem), 69rem)`) holding `GeoMap` or, at school level, `StudentCards`, plus the up-a-level button bottom-right (disabled at the user's own level); "{Noun} Detail" (`RepMeta`); "{Noun} Performance" (`bg-blue-50`; second `MvpMetricToggle`; not-using headline above school level; "Trend" row with `MvpRangeBar` on the right, `RepTrend`; "Most improved · last {range} days" `RepImproved`); "{Officer} Spotlight" (two `RepQuote`s); "Your Profile" (`bg-blue-50`); footer card. There is NO profile header, NO share bar and NO "What is Lifteracy?" link — mvp2 has none (the link IS the page URL).
3.) Data: on every change of (current entity id, metric, range) fetch `GET geo-entities/:id/scores?metric=&range=` and `GET geo-entities/:id/spotlight?metric=&range=` in parallel (spotlight failure is tolerated → null). Results are keyed by `id|metric|range` so stale responses never render. Defaults: metric `nipun_g3`, range 30.
4.) Root card: mvp2's `RepKpis` — ONE card "{pass_rate as whole %}" coloured by `nipColor` with label "average {metric label}", plus "{usingN} of {totalN} {nounP} using Lifteracy" above school level only. No students-active / change / as-of tiles (the change arrow lives in the Detail card). When `root.n` is null render exactly "No results yet — share your link to get started." instead of numbers (header stays; Detail/Performance/Spotlight hidden; map card still shown).
5.) Drill-down: `stack: GeoRef[]` seeded with `profile.geo_entity`; double-click on a map area/label/dot, chart bar/line or the detail card pushes the child; the ⤒ button in the map card pops (mvp2 replaced zoom with it; there is no breadcrumb under the title). Cannot go above the user's own entity. Levels country → state → district → block → school. Block-level district outline comes from the previous stack entry or, when the root itself is a block, from `profile.ancestors`. Single click on a map area / chart item selects it (`selId`) for the Detail card; clicking the Detail card opens `MvpTeacherModal`.
6.) Nouns: `CHILD_NOUN` / `CHILD_OFFICER` of the current `child_type` (school → "Student" / "Teacher"; before scores load, a school entity uses the student nouns so the nav never flashes "Area").
7.) `StudentCards` (school level, inside the map card): mvp2's teacher-row list — `absolute inset-0 overflow-y-auto p-3 sm:p-5`, `max-w-3xl` column of rows tinted `nipColor(score) + "1f"` / border `+ "55"`: plant avatar ring (`seed = student_id`), label, "Student · {attempts} attempts", score as whole % + metric short (right on ≥sm, under the name on phones), `MvpTrend` (delta is null until pp-sketch exposes a per-student delta → grey "—"). Single click pins the row into the Detail card (`selId`), double-click opens the modal in student mode; clicking the pane background clears the pin. "No students yet." when empty. No table.
8.) Profile editor (`PATCH users/:id/profile`, only changed fields): mvp2's 3-column grid — avatar `h-36 w-36` (ring 4 px accent + drop shadow), "Shuffle avatar" (`w-full max-w-[12rem]` blue button; mvp2's "Upload profile photo" slot) → `randomSeed()` → optimistic preview → PATCH `{ avatar_seed }` (reverts on failure); Display name (≤80), Spotlight message textarea (maxLength 300, no counter — mvp2 has none), preview card, "Save" (blue; disabled until something changed — mvp2's demo has no save step, the real profile needs one). Success "Saved"; failure "Not saved — {server message}" (Nest `{ message }` string or string[] joined "; ", else `HTTP <status>`).
9.) Report: "Generate report" opens `ReportCardModal` with the data already on the page (title, share link, metric, range, as_of, root, series, children, most_improved, spotlight) at EVERY level, school included.
10.) Language: `lang` state ("en" default so SSR matches), read from `localStorage["lifteracy-dashboard-lang"]` in a deferred effect (setTimeout 0 — never setState synchronously in an effect), written on toggle; `t = makeT(lang)` (i18n.ts) is passed down to every block. Untranslated strings fall back to English.
11.) Wording that differs from mvp2 on purpose, because the API window is 30/90 days, not weeks: "Trend" (not "Weekly trend"), "Most improved · last {range} days" (not "this week"), trend arrows "+1.8% last {range} days".
12.) Lint constraints: never setState synchronously in an effect body; no ref reads during render.
