1.) "use client" `TeacherDashboard({ profile, incompleteStates, explainerUrl, avatarSvg })` — port of the mvp2.html "report" layout (sandbox-global-map/mvp2.html). Every number comes from pp-sketch through `/api/proxy/...` (public allowlist in src/app/api/proxy/[...path]/public-allowlist.ts). No dummy/hash-seeded data anywhere.
2.) Layout, top to bottom: sticky bar (Lifteracy logo, section anchors Top / {Noun} Performance / {Officer} Spotlight / Your Profile, "Generate report" button, accent `#1d9edf`); profile header (avatar — the server-rendered SVG while the seed is unchanged, else `/api/avatar/...` —, name as `<h1>`, role_title · geo name, ancestor breadcrumb root-first); `MvpShareBar`; location title (ancestors + drill stack joined "  -  ") with clickable drill breadcrumb and "Up a level" (only when drilled); `MvpMetricToggle` + `MvpRangeBar`; root card; map (or student table at school level); "{Noun} Detail" (`RepMeta`); "{Noun} Performance" (not-using headline, `RepTrend` from `series`, `RepLatest`, sortable children table, `RepImproved`); "{Officer} Spotlight" (`RepQuote` top + most improved from `/spotlight`); "Your Profile" editor; footer.
3.) Data: on every change of (current entity id, metric, range) fetch `GET geo-entities/:id/scores?metric=&range=` and `GET geo-entities/:id/spotlight?metric=&range=` in parallel (spotlight failure is tolerated → null). Results are keyed by `id|metric|range` so stale responses never render. Defaults: metric `nipun_g3`, range 30.
4.) Root card: `RepKpis` shows pass rate WITH n beside it ("72.0% · n=41"), students_active, `MvpTrend(delta)` with "last {range} days", "{usingN} of {totalN} {nounP} using Lifteracy", and "as of {as_of}". When `root.n` is null render exactly "No results yet — share your link to get started." instead of numbers (header + share block stay; charts/detail/spotlight hidden; map still shown).
5.) Drill-down: `stack: GeoRef[]` seeded with `profile.geo_entity`; double-click on a map area/label/dot, chart bar/line, table row or the detail card pushes the child; breadcrumb / Up pops. Cannot go above the user's own entity. Levels country → state → district → block → school; at school the student table replaces the map. Block-level district outline comes from the previous stack entry or, when the root itself is a block, from `profile.ancestors`.
6.) Children table: columns Name · {Officer} (official.name) · Pass rate (bin colour chip: high/mid/low via `binColor`, grey `#a1a1aa` when not using) · Δ · n · Active; sortable by pass_rate / delta / n (click header, default pass_rate desc, nulls last); wrapped in `overflow-x-auto` so it works at 360 px. Single click on ANY row opens `MvpTeacherModal` with `child.official`; double-click drills.
7.) Student table (school level): Student · metric short · Result · Attempts · In band · Active · Last active (IST); click opens the modal in student mode.
8.) Profile editor (`PATCH users/:id/profile`, only changed fields): Display name (≤80), Spotlight message textarea (maxLength 300) with a live "{len}/300" counter (red at 300), preview card, "Save" (disabled until something changed), "Shuffle avatar" → `randomSeed()` → optimistic preview via `/api/avatar/sprouts-v10/{seed}` → PATCH `{ avatar_seed }` (reverts on failure). Success shows "Saved" and replaces the profile; failure "Not saved — {server message}" (Nest `{ message }` string or string[] joined "; ", else `HTTP <status>`).
9.) Report: "Generate report" opens `ReportCardModal` with the data already on the page (title, share link, metric, range, as_of, root, series, children, most_improved, spotlight). Disabled at school level.
10.) Lint constraints: never setState synchronously in an effect body; no ref reads during render.
